# these are requirements for the whole repository
# there are requirements-download.txt and requirements-web.txt
# if you only use those parts.

Jinja2>=2.7.3, <3.0
MarkupSafe==0.23
requests>=2.3.0
matplotlib==1.4.3
numpy==1.9.2
tqdm>=3.7.1, <5.0  # tested up to 4.4.0
# simplejson is optional but usually speeds up normalize/merge/analysis scripts a bit
