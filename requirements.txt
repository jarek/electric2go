# these are requirements for the whole repository
# there are requirements-download.txt and requirements-web.txt
# if you only use those parts.

Jinja2==2.7.3
MarkupSafe==0.23
requests>=2.4.1
wsgiref==0.1.2
matplotlib
numpy
# simplejson is optional but usually speeds up normalize/merge/process scripts a bit
